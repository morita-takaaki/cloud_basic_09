# Java + SQLite + Docker Sample
## How to run
1. Clone this repository.2. Create student.db using sqlite3.3. Build Docker image:
   docker build -t sqlite-java-app .4. Run container:
   docker run sqlite-java-app
